# Case ACME-2026-0007 — injury absence, Jordan Rivera

The **administrative** record of an employee absence: what the company owns —
work status, restrictions, dates, accommodations. Clinical detail lives in the
clinic's chart (`example/patient`); wages and job history live in payroll's
employee file (`example/ee`). This case **references** both and copies neither.

Read [case.mdy](case.mdy) for current status, [timeline.md](timeline.md) for
the event log. Each stage of this pod is a tag: `intake`, `visit-1`,
`visit-2`, `closed`.
